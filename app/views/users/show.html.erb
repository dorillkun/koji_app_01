<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="<%= "/user_images/#{@user.image_name}" %>" >
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <%= link_to("アカウント編集", "/users/#{@user.id}/edit") %>
    </div>
  </div>
</div>